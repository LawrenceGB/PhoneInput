{
  "name": "phone-number-with-country-code",
  "version": "2.0.4",
  "description": "react native component to create phone number input with country code.",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },

  "author": "Lawrence",
  "license": "ISC",

  "dependencies": {
    "react-native-elements": "^3.4.2",
    "react-native-gesture-handler": "^2.5.0"
  }
}
